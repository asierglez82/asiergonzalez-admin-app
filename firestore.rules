rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Reglas para la colección CRM
    match /crm_contacts/{contactId} {
      // Permitir lectura y escritura a usuarios autenticados
      allow read, write: if request.auth != null;
      
      // Subcolecciones heredan las mismas reglas
      match /notes/{noteId} {
        allow read, write: if request.auth != null;
      }
      
      match /reminders/{reminderId} {
        allow read, write: if request.auth != null;
      }
      
      match /interactions/{interactionId} {
        allow read, write: if request.auth != null;
      }
    }
    
    // Reglas para otras colecciones existentes
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}

// Reglas temporales para desarrollo (descomenta si necesitas más permisos)
// rules_version = '2';
// service cloud.firestore {
//   match /databases/{database}/documents {
//     match /{document=**} {
//       allow read, write: if true;
//     }
//   }
// }
